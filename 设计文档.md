# MegaDesc 设计文档

> 本文描述当前已实现的做法（对应 `MegaDesc.cpp` + `modules/*.inc.cpp` 实际代码）。

## 1. 功能概述

本插件扩展生物信息窗口：加高窗口、替换背景/按钮素材、扩大描述文本区、显示第二行战斗价值，兼容 H3.TextColor 长文本颜色标签，并为通用滚动文本对话框提供鼠标滚轮支持。

触发方式：任意界面打开生物信息窗口（战斗/冒险/城镇）。

通过 exe patch 把窗口从原版 311 扩展到 383，并替换背景/按钮素材。

## 2. 窗口扩展与素材替换

### 2.1 exe patch

6 个地址（三界面×2），把原版 `push 0x137`(311) 改为 INI 配置高度（默认 383）：

| 界面  | patch 地址           |
| --- | ------------------ |
| 战斗  | 0x5F3F1B, 0x5F406B |
| 冒险  | 0x5F3728, 0x5F38CF |
| 城镇  | 0x5F45D8, 0x5F4712 |

### 2.2 PCX 素材替换

插件目录 `pcx\` 下放置标准 PCX 素材（背景支持 8 位索引或 24 位三平面 PCX，按钮素材为 24 位三平面 PCX）。插件自行 RLE 解码 + 调色板量化为 `_Pcx8_*`，替换原控件的内部图片指针。不新建控件、不换 vtable。

### 2.3 描述文本控件

原版描述控件通过 `_DlgStaticText_::Create(0x5BC6A0)` 创建，参数由 `push` 指令压栈。`TextHeight > 127` 时不能用原版 `push imm8`（符号扩展问题），改为在 call 前 LoHook 改写栈参数（`x/y/w/h`），让创建函数收到真实 32 位值。

### 2.4 窗口 Y 吸附

扩展后的窗口可能超出 HD 渲染 buffer。在 `_Dlg_` 初始化入口 `0x41AFA0` LoHook 中按 HD 分辨率高度（从 `sod.ini` 的 `<Graphics.Resolution>` 读取）夹窗口 Y 值，防止 ZCN2 绘制越界。

### 2.5 战斗价值第二行

战斗价值显示由 MegaDesc 在三个生物信息窗口 BUILD Hook 中创建，控件 ID 为 `3009`（标签）和 `3008`（数值）。主值来自全局生物表 `0x6747B0` 中每个 `0x74` 字节记录的 `+0x3C`；战斗中若能唯一匹配当前 stack，则在括号中显示按队首剩余 HP 折算的单只价值。

Y 坐标为名称控件 `id=203` 的 Y 加 `FightValueYOffset`，默认偏移 19。标签默认文本为“战斗价值”，数值右对齐。

### 2.6 通用滚动文本鼠标滚轮支持

右键查看魔法详情等场景弹出的 `_DlgScrollableText_` 控件，其内部滚动条不接收鼠标滚轮消息——`_Dlg_::DefProc` 默认直接返回 `TRUE` 吞掉滚轮事件。

当 `[Features] EnableRightClickScroll=1` 时，MegaDesc 使用 `WH_GETMESSAGE` 线程钩子在 Windows 消息泵层拦截 `WM_MOUSEWHEEL`，因为原版 H3 消息循环不会把该消息转成 `_Dlg_::DefProc` 可处理的 `MOUSE_WHEEL`。插件同时 Hook `_DlgScrollableText_::Create(0x5BA360)`，只缓存最近由右键触发弹窗创建的滚动文本控件；若最近是左键触发，则清空缓存，避免干预左键弹窗已有的原生滚动。该功能默认关闭。

滚轮处理时读取 `_DlgScrollableText_ + 0x54` 的内部 `H3DlgTextScrollbar`，通过 `SetTick(0x5964D0)` 修改 `tick` 与滑块位置数据，设置滚动条 dirty flag (`+0x16 |= 1`)，调用底层滚动条渲染函数 `0x596F40` 刷新滑块，再清除 dirty flag，最后调用 vtable `+0x40` 的 `vScrollCallOwner` 通知文本控件更新内容。边界按 `ticksCount(+0x48)` 限制；成功路径不写日志，仅保留异常日志。已有生物信息窗口布局兜底逻辑不受影响。

### 2.7 H3.TextColor 滚动文本颜色继承

`H3.TextColor.dll` 负责渲染 `{~Color}`/`#RRGGBB` 等颜色标签，但 `_DlgScrollableText_` 会先通过 `H3Font::SplitTextIntoLines(0x4B58F0)` 把长文本拆成多条 `H3String` 行。颜色标签跨行时，后续行会失去当前活动颜色，导致魔法详细描述等长文本在换行后回到默认色。

当 `[Features] EnableTextColorFix=1` 时，MegaDesc 在 `0x4B58F0` 加 HiHook：先调用当前 hook 链的默认拆行实现，再扫描拆分后的行。若上一行仍处于 `{~Color}` 状态，则给下一行补同一个颜色标签前缀；遇到 `}` 时清空活动颜色。这样不替换 `H3.TextColor` 的绘制逻辑，也不把它作为硬依赖。该功能默认关闭。

## 3. Hook 列表

| Hook        | 地址                         | 类型     | 说明                 |
| ----------- | -------------------------- | ------ | ------------------ |
| BUILD hooks | 0x5F4503/0x5F3E75/0x5F491E | LoHook | 三界面生物信息窗口构建、布局调整和战斗价值插入 |
| DefProc     | 0x41B120                   | HiHook | 生物信息窗口布局兜底调整 |
| ScrollableTextCreate | 0x5BA360 | LoHook | 缓存右键弹窗创建的滚动文本控件 |
| DescCreate  | 0x5F447F/0x5F3E54/0x5F489A | LoHook | 三界面描述控件 call 前改栈参数 |
| DlgInit     | 0x41AFA0                   | LoHook | 窗口 Y 吸附            |
| WH_GETMESSAGE | Win32 线程钩子 | 消息钩子 | `EnableRightClickScroll=1` 时拦截右键弹窗的 `WM_MOUSEWHEEL` 并驱动滚动条 |
| TextColorSplitLines | 0x4B58F0 | HiHook | `EnableTextColorFix=1` 时拆行后补齐 H3.TextColor 跨行颜色状态 |

## 4. 配置文件 MegaDesc.ini

```ini
[Logging]
; 0 = 不禁止，正常写日志并保留最近 30 个（默认）
; 1 = 禁止，不生成日志，也不扫描/清理旧日志
DisableLog=0

[Features]
; 右键魔法描述框滚轮翻页：0=关闭，1=启用，默认关闭
EnableRightClickScroll=0
; 滚动文本跨行颜色补丁：0=关闭，1=启用，默认关闭
EnableTextColorFix=0

[Layout]
WindowHeight=383
Shift=13
DescY=248
TextHeight=105
TextWidth=200
DismissBtnMarginBottom=90
UpgradeBtnMarginLeft=230
UpgradeBtnMarginBottom=181
FightValueYOffset=19

[Images]
BackgroundPcx=bv_bg.pcx

[Format]
LabelFightValue=战斗价值
```

## 5. 技术约束

| 约束   | 说明                                      |
| ---- | --------------------------------------- |
| 目标版本 | 英雄无敌3 死亡阴影（SoD），HotA + SoD_SP 环境        |
| 位数   | 仅 x86                                   |
| 结构对齐 | `/Zp1`                                  |
| 字符集  | `/utf-8`                                |
| 插件名  | `HD.Plugin.MegaDesc`                  |
| 依赖   | 原版游戏 EXE + HD Mod 框架（`patcher_x86.hpp`） |

## 6. 源码结构

| 文件                                 | 说明                                      |
| ---------------------------------- | --------------------------------------- |
| `MegaDesc.cpp`                   | 主文件：全局 `_P`/`_PI` + 7 个 `#include`      |
| `modules/ConfigLog.inc.cpp`        | Config 结构体、INI 读取、日志系统                  |
| `modules/CreatureDialog.inc.cpp`   | 核心功能：窗口布局调整、按钮替换、BUILD/DefProc hooks    |
| `modules/fightvalue.inc.cpp`       | 生物信息窗口第二行战斗价值计算与显示                  |
| `modules/TextColorScrollFix.inc.cpp` | H3.TextColor 滚动文本拆行后的跨行颜色状态补齐 |
| `modules/PatchesAndImages.inc.cpp` | 窗口高度 exe patch、8/24-bit PCX 加载/量化/合成、按钮替换 |
| `modules/Entry.inc.cpp`            | `StartPlugin()` 入口、`DllMain`、hook 注册    |
